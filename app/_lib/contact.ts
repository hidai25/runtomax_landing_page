/**
 * The single address users are told to write to. It appears in the footers, the
 * privacy policy's GDPR/CCPA section, the JSON-LD contact point, and the support
 * page — so it must be a mailbox that is actually read, not one that merely looks
 * official. Switching to hello@runtomax.com means changing this line and
 * public/llms.txt, which is not generated from this file.
 */
export const CONTACT_EMAIL = "hidai25@gmail.com";

export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}`;
